import { Box } from '@chakra-ui/react';
import React from 'react';

export const Chao = () => {
    const props = { border: '4px' };

    return (
        <Box mt={1} mr={2} mb={1}>
            Same Priorities!
        </Box>
    );
};

export default Chao;
